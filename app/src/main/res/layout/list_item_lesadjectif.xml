<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/elm"
    android:padding="16dp">

    <ImageView
        android:id="@+id/img_logo_adjectif"
        android:layout_width="@dimen/_50sdp"
        android:layout_height="@dimen/_50sdp"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/tv_title_adjectif"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/_15sdp"
        android:layout_toRightOf="@id/img_logo_adjectif"
        android:ellipsize="end"
        android:fontFamily="@font/warung_kopi"
        android:lines="1"
        android:maxLines="1"
        android:text="Le Verbe Reguliere"
        android:textAllCaps="true"
        android:textColor="@color/white"
        android:textSize="@dimen/_20ssp" />
</RelativeLayout>